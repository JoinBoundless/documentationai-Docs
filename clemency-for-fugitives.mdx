---
title: Cyber Sovereignty
---

import { useState } from "react"

# Cyber Sovereignty

## Navigation

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 mt-6">

<button onClick={() => setTab("overview")} className="card">Overview</button>
<button onClick={() => setTab("charter")} className="card">Charter</button>
<button onClick={() => setTab("legal")} className="card">Legal Mechanisms</button>
<button onClick={() => setTab("articles")} className="card">Articles</button>
<button onClick={() => setTab("experimental")} className="card">Experimental</button>

</div>

---

export default function Page() {
  const [tab, setTab] = useState("overview")

  return (
    <div>

      <div className="grid grid-cols-1 md:grid-cols-2 gap-4 mt-6">
        <button onClick={() => setTab("overview")} className="card">Overview</button>
        <button onClick={() => setTab("charter")} className="card">Charter</button>
        <button onClick={() => setTab("legal")} className="card">Legal Mechanisms</button>
        <button onClick={() => setTab("articles")} className="card">Articles</button>
        <button onClick={() => setTab("experimental")} className="card">Experimental</button>
      </div>

      <div className="mt-10">

        {tab === "overview" && (
          <>
            <h2>Overview</h2>
            <p>This is the overview section. Add your content here.</p>
          </>
        )}

        {tab === "charter" && (
          <>
            <h2>Charter</h2>
            <p>This is the charter section content.</p>
          </>
        )}

        {tab === "legal" && (
          <>
            <h2>Legal Mechanisms</h2>
            <p>Explain legal frameworks here.</p>
          </>
        )}

        {tab === "articles" && (
          <>
            <h2>Articles</h2>
            <p>All articles content goes here.</p>
          </>
        )}

        {tab === "experimental" && (
          <>
            <h2>Experimental</h2>
            <p>Experimental ideas and drafts.</p>
          </>
        )}

      </div>

    </div>
  )
}

<style jsx>{`
.card {
  padding: 20px;
  border-radius: 10px;
  background: #1f1f1f;
  text-align: center;
  cursor: pointer;
}
.card:hover {
  background: #2a2a2a;
}
`}</style>